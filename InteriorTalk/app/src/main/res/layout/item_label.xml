<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <!-- 레이블 넘버 레이아웃 -->
    <FrameLayout
        android:theme="@style/ThemeOverlay.AppCompat.Light"
        android:id="@+id/layout_label_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="10dp">

        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:contentDescription="@string/label_number"
            android:src="@drawable/ic_circle" />

        <!-- 레이블 넘버 -->
        <TextView
            android:id="@+id/txt_label_number"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="7"
            android:theme="@style/ThemeOverlay.AppCompat.Dark"
            tools:ignore="HardcodedText" />

    </FrameLayout>

    <!-- 레이블 제목 -->
    <TextView
        android:id="@+id/txt_label_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toStartOf="@id/txt_purchase_url"
        android:layout_toEndOf="@id/layout_label_number"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="에이스 침대"
        tools:ignore="HardcodedText" />

    <!-- 구매 링크 -->
    <TextView
        android:id="@+id/txt_purchase_url"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:text="www.coupang.com"
        tools:ignore="HardcodedText" />

</RelativeLayout>